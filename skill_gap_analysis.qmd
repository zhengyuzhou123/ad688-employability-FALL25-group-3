---
title: "Skill Analysis"
subtitle: "Skill Gap Analysis"
author:
  - name: "Bingrui Qiao"
  - name: "Zhengyu Zhou"
  - name: "Junhao Wang"
    affiliations: "Boston University"
bibliography: references.bib
csl: csl/econometrica.csl
format:
  html:
    toc: true
    number-sections: true
execute:
  echo: true
  eval: true
  freeze: false
  error: false
  cache: false
  enabled: !expr (os.getenv("CI", "false") != "true")
jupyter: python3
---
# Create a team-based skill dataframe
```{python}
import pandas as pd

skills_data = {
    "Name": ["Bingrui Qiao", "Zhengyu Zhou", "Junhao Wang"],
    "Python": [2, 3, 3],
    "SQL": [2, 3, 3],
    "Machine Learning": [1, 1, 1],
    "Cloud Computing": [2, 2, 3],
    "Docker": [1, 3, 1],
    "AWS": [2, 3, 2]
}

df_skills = pd.DataFrame(skills_data)
df_skills.set_index("Name", inplace=True)
df_skills
```
# Visualizing Skill Gaps
```{python}
import seaborn as sns
import matplotlib.pyplot as plt

#visual
sns.set_theme(style="whitegrid", font_scale=1.1, rc={
    "axes.facecolor": "white",
    "figure.facecolor": "white",
    "axes.edgecolor": "gray",
    "grid.color": "lightgray"
})

# Color choice
cmap = sns.diverging_palette(220, 20, as_cmap=True)

# visual
plt.figure(figsize=(9, 6))
ax = sns.heatmap(
    df_skills,
    annot=True,
    fmt=".0f",
    cmap=cmap,
    linewidths=0.7,
    cbar_kws={'label': 'Skill Level'},
    square=True
)

# Title
plt.title("Team Skill Levels Heatmap", fontsize=15, weight="bold", pad=20)
plt.xlabel("Skill Domain", fontsize=12)
plt.ylabel("Team Member", fontsize=12)

# layout
plt.xticks(rotation=30, ha="right")
plt.yticks(rotation=0)
plt.tight_layout()

#save
plt.savefig("figures/Skill_Gap_Analysis.png", dpi=300, bbox_inches="tight")
plt.show()
```

# Compare skill
```{python}
# Load data
raw_df = pd.read_csv("data/cleaned_lightcast.csv")

# Count keyword occurrences
top_skills = df_skills.columns.tolist()
job_text = raw_df["BODY"].fillna("")
skill_counts = {s: job_text.str.contains(s, case=False).sum() for s in top_skills}

# Append demand row
df_skills.loc["Market Demand"] = [skill_counts[s] for s in top_skills]
df_skills
```
# Visual heatmap
```{python}
import seaborn as sns
import matplotlib.pyplot as plt

# Same style
sns.set_theme(style="whitegrid", font_scale=1.1, rc={
    "axes.facecolor": "white",
    "figure.facecolor": "white",
    "axes.edgecolor": "gray",
    "grid.color": "lightgray"
})


cmap = sns.diverging_palette(220, 20, as_cmap=True)

# Team Skill Levels
plt.figure(figsize=(8, 2.5))
plt.imshow(df_skills.iloc[:-1], aspect="auto", cmap=cmap)
plt.colorbar(label="Skill Level (1–5)")
plt.yticks(range(len(df_skills.index)-1), df_skills.index[:-1], fontsize=10)
plt.xticks(range(len(df_skills.columns)), df_skills.columns, rotation=30, ha="right", fontsize=9)
plt.title("Team Skill Levels", fontsize=13, weight="bold")
plt.tight_layout()
plt.savefig("figures/Team_Skill_Levels.png", dpi=300, bbox_inches="tight")
plt.show()

# Market Demand Heatmap
plt.figure(figsize=(8, 2))
plt.imshow([df_skills.loc["Market Demand"]], aspect="auto", cmap=cmap)
plt.colorbar(label="Market Demand Count")
plt.yticks([0], ["Market Demand"], fontsize=10)
plt.xticks(range(len(df_skills.columns)), df_skills.columns, rotation=30, ha="right", fontsize=9)
plt.title("Market Demand by Skill", fontsize=13, weight="bold")
plt.tight_layout()
plt.savefig("figures/Market_Demand.png", dpi=300, bbox_inches="tight")
plt.show()
```

Based on the heatmap comparison of each person’s self-ratings (1–3) against the market-demand scores (SQL and Machine Learning are hottest; Cloud/Docker mid; AWS/Python lower), here’s a concise, paragraph-style improvement plan:

Bingrui Qiao should prioritize Machine Learning and SQL. Start with Kaggle’s “Intro to ML” + scikit-learn tutorials (train/validate linear/logistic/trees), then solve Mode SQL and LeetCode Database tracks to raise SQL from 2→3. Package one ML notebook into a small FastAPI service and containerize it to practice Docker basics (images, multi-stage build, docker run). Target two 90-minute focus blocks per week and submit progress as GitHub PRs for review. 

Zhengyu Zhou is already strong in Python/SQL/Docker/AWS; the biggest gap is Machine Learning and, secondarily, Cloud architecture depth. Take Andrew Ng’s “Machine Learning Specialization,” then deploy a trained model as a container on AWS (ECS Fargate or Lambda + API Gateway). For Cloud (2→3), complete AWS Well-Architected + IAM/VPC labs and add IaC (SAM/CloudFormation/Terraform) to the repo. After each module, demo a working endpoint and post a runbook; ask a teammate for a security and cost review. 

Junhao Wang is solid in Python/SQL/Cloud but needs Machine Learning and Docker. Follow fast.ai (first 2–3 lessons) or scikit-learn’s official guides to lift ML (1→2/3). In parallel, complete “Docker Get Started,” containerize a simple inference API, push to Amazon ECR, and run on ECS; add a GitHub Actions workflow to build/test/push on every PR. Aim for one 2-hour ML block and one 2-hour Docker/DevOps block weekly; measure success by latency/throughput of the deployed service and a clean CI pipeline.



